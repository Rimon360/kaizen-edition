# Copy to `.env` and fill in real values. NEVER commit the real .env — it holds
# the live API + HMAC request-signing secrets and the publish repo is public.
VITE_API_URL=
VITE_API_SECRET=
VITE_HMAC_SECRET=
VITE_CLIENT_ID=
